import React from "react";

const Loading = () => {
  return (
    <div>
      <span className="loading loading-bars loading-xl bg-gradient-to-r from-purple-500 via-pink-500 to-red-500 rounded-full animate-pulse"></span>
    </div>
  );
};

export default Loading;
